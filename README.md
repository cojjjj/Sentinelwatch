# Sentinelwatch
It’s called SentinelWatch, and it’s a file integrity monitor that:  creates a baseline of file hashes scans a folder later for added, removed, or modified files ignores common junk folders like .git, venv, and node_modules exports results to JSON uses only the Python standard library 
